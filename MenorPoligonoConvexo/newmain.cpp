//Fucoes que temporariamente nao estou usando
/*
 //typedef std::list <float>            ListaPonto;
 
 //    ListaPonto     Pontos;
//    ListaPonto     PontosPoli;
 
 * //    Ponto      P;
//    
//    for (int i=0; i<5; i++)
//    {    
//        P.set(x,y);
//        Pontos.push_back(P);
//    }
 *  
         //Adicionando os pontos gerados na lista de pontos gerais
//        Pontos.push_back (x);
//        Pontos.push_back (y);
 
     //Imprimi os pontos da lista na tela
//    std::cout << "Pontos da lista" << std::endl;
//    auto inicio = std::begin(Pontos); std::advance(inicio, 0);
//    auto fim = std::begin(Pontos); std::advance(fim, -1);
//    
//    for (auto x = inicio; x != fim; ++x)
//    {
//        std::cout << *x << "\n";
//    }
 * 
 * 
 * 
 * //    for (auto& x: ListaPonto)
//    {
//        if (x.first)
//    }    
//    
    //list<int>::iterator it_max = max_element(ListaPonto.begin(), ListaPonto.end());
   //auto inicio = std::begin(Pontos); std::advance(inicio, 0);
  // auto fim = std::begin(Pontos); std::advance(fim, -1);
   std::list <int> std::iterator auto it_min = min_element(ListaPonto.begin(), ListaPonto.end());
   std::cout << "Min: " << *it_min << std::endl;
 
 
 
 //    
//    for (auto x = inicio; x != fim; ++x)
//    {
//        std::cout << "Min: " << *it_min << std::endl;
//    }
 
    
//        for( int i = 1 ; i < iNumPontos ; i++ )
//{
//if( tPontos[i].y > tPontos[0].y )
//{
  
 
 */
